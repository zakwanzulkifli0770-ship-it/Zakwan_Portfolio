# Server
NODE_ENV=production
PORT=10000
BASE_PATH=/

# PostgreSQL — provided by Render or your own database
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Admin dashboard password (change this before deploying!)
ADMIN_PASSWORD=change_me_before_deploying
